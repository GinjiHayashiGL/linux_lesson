# CLAUDE.md

## プロジェクト概要

Linux カスタムコマンドを C で実装しながらシステムプログラミングを学ぶリポジトリ。

## ディレクトリ規則

新しいコマンドはセクションごとにディレクトリを切る。

```
src/
└── sectionN/
    ├── <command>.c
    ├── <command>.md
    └── test_files/
        ├── test_file_a.txt
        └── test_file_b.txt
```

## .md ドキュメント生成規則

**新しいコマンドを実装するたびに、対応する `.md` ファイルを必ず生成すること。**

### 構成（必須セクション）

1. **タイトルと概要文**
   - `# <filename>.c 解説` をタイトルとする
   - コマンドの一言説明を続ける
   - 参考にした既存実装がある場合はその違いも明記する

2. **## 概要**
   - 使用方法をコードブロックで示す
   ```
   ./<command> <引数> [引数 ...]
   ```

3. **## インクルードファイル**
   - 使用するヘッダを表形式で記載する
   - 列: `ヘッダ` / `概要` / `用途`

4. **## 関数**
   - 実装した関数ごとにサブセクション（`###`）を設ける
   - 各関数はシグネチャをコードブロックで示した後、コード断片ごとに箇条書きで日本語解説する
   - `static` / `const` などの修飾子の意図も説明する
   - 関数内で呼び出している標準ライブラリ関数・システムコールについても、その箇条書き解説の中で以下を明記する
     - シグネチャ（例: `int getc(FILE *stream)`）
     - 戻り値の意味と、エラー時の挙動
     - なぜその関数を選んだか（設計意図）

5. **## 処理の流れ**
   - コードブロック内に ASCII のコールフロー図を描く

6. **## 関数・システムコール一覧**
   - 使用した関数を表形式でまとめる
   - 列: `関数（またはシステムコール）` / `シグネチャ` / `説明`

7. **比較が有意義な場合のみ: ## <既存実装> との比較**
   - 観点・旧実装・新実装の3列表で差異を整理する

### 書き方の方針

- 解説はすべて日本語で書く
- コードブロックは実際のソースから抜粋し、改変しない
- 「なぜその設計か」の理由まで踏み込んで説明する
- 初学者が読んでも理解できる粒度を意識する
- 余分な見出しや空セクションは作らない

### 粒度の基準

**`grep.md` の粒度を常に基準とすること。** 具体的には：

- `## 関数` セクションでは、関数内のコードを意味単位の小さなスニペットに分割し、各スニペットの直後に箇条書きで解説する
- 呼び出している関数ごとに **シグネチャ・戻り値（エラー時の挙動を含む）・設計意図** を必ず記載する
- 変数宣言のスニペットも独立させ、各変数の役割を説明する
- セクション間には `---` で水平線を入れる

### 参考ファイル

- [src/section8/grep.md](src/section8/grep.md) — **粒度の基準となる記述例**（コードスニペット分割＋関数シグネチャ詳細）
- [src/section5/cat.md](src/section5/cat.md) — POSIXシステムコール版の記述例
- [src/section6/cat2.md](src/section6/cat2.md) — 標準ライブラリ版＋比較表の記述例
